#include "Reversi.hpp"
#include <iomanip>
#include <iostream>
#include <string>

/*
Reversi::Reversi() {
  this -> rows = 8;
  this -> columns = 8;
}

void Reversi::inicializaTabuleiro() {
  //lógica
}

void Reversi::defineJogada() {
}

void Reversi::validaJogada() {
  3 casas: black, white, empty
  black++ //usar apontadores aqui talvez???
  white++
}

int Reversi::confereGanhador {

//primeiro caso: tabuleiro totalmente preenchido, jogador com mais peças vence

  for (int i = 0; i < rows; i++) { 
    for (int j = 0; j < columns; j++) {
      if (não existir mais nenhuma casa empty) {
        black > white ? return 1 : return 2;
      }
    }
  }

//segundo caso: tabuleiro não totalmente preenchido, mas não há mais jogadas possíveis para um dos jogadores, jogador que tem jogadas disponíveis continua até que preencha todo o tabuleiro ou fique sem jogadas disponíveis e passa pro próximo, talvez o empate também se encaixe aqui, conferir

//terceiro caso: tabuleiro totalmente preenchido, empate, mesmo número de peças

  for (int i = 0; i < rows; i++) { 
    for (int j = 0; j < columns; j++) {
      if (black == white) {
        return 3;
      }
    }
  } 
*/